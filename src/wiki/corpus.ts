/**
 * 오프라인 폴백용 LLM 위키 코퍼스.
 *
 * [초보자 설명] 왜 로컬 사본을 들고 있는가?
 * `npm run wiki:ingest` 는 기본적으로 한국어 위키백과 API 에서 문서를 받아온다.
 * 그런데 강의장 네트워크가 막혀 있거나 위키백과가 응답하지 않으면 실습 자체가 멈춘다.
 * 그래서 같은 모양(마크다운 헤딩 구조)의 문서를 여기에 넣어두고, 네트워크가 안 되면
 * 조용히 이쪽으로 넘어간다. 파이프라인 뒷부분(청킹 → 임베딩 → 검색)은 완전히 똑같이 돈다.
 *
 * [자바 노트] 이런 "테스트/폴백용 고정 데이터"를 자바에서는 보통 resources 의 json 파일로 두지만,
 *            TS 에서는 그냥 모듈에 const 로 두는 편이 타입까지 같이 잡혀서 편하다.
 */
export interface WikiArticle {
  /** 문서 제목. 위키백과에서 받아올 때의 검색어이기도 하다. */
  title: string
  /** 원문 주소. 검색 결과에 출처로 함께 보여준다. */
  url: string
  /** `##` 헤딩으로 구획된 마크다운 본문. */
  markdown: string
}

/** 실습 기본 주제. 위키백과에서 받아올 때도 이 제목들을 그대로 조회한다. */
export const WIKI_TITLES = ["대규모 언어 모델", "트랜스포머 (기계 학습)", "단어 임베딩"] as const

export const LOCAL_WIKI_ARTICLES: WikiArticle[] = [
  {
    title: "대규모 언어 모델",
    url: "https://ko.wikipedia.org/wiki/대규모_언어_모델",
    markdown: `## 개요
대규모 언어 모델(LLM, Large Language Model)은 방대한 텍스트 말뭉치로 사전 학습된 신경망 언어 모델을 말한다.
매개변수 규모가 수십억에서 수천억 개에 이르며, 다음 토큰을 예측하는 단일 목표만으로 번역, 요약, 코드 생성 등
여러 작업을 별도 학습 없이 수행할 수 있다는 점이 기존 언어 모델과 구별되는 특징이다.

## 학습 단계
LLM 학습은 보통 세 단계로 나뉜다. 사전 학습(pre-training)은 대규모 웹 문서와 서적 말뭉치로 다음 토큰 예측을
반복하는 단계로, 전체 연산량의 대부분을 차지한다. 지도 미세 조정(SFT)은 사람이 작성한 질문-답변 쌍으로
지시를 따르는 형식을 익히는 단계다. 마지막으로 사람 선호 기반 강화 학습(RLHF)에서는 사람이 매긴 응답 선호도로
보상 모델을 만들고, 그 보상을 최대화하도록 정책을 조정한다.

## 토큰과 컨텍스트 윈도
LLM 은 글자가 아니라 토큰(token) 단위로 텍스트를 처리한다. 영어는 대략 단어 하나가 1~2 토큰이지만,
한국어는 조사와 어미 때문에 같은 의미라도 토큰 수가 2~3배 많아지는 경향이 있다.
한 번에 처리할 수 있는 토큰의 최대 개수를 컨텍스트 윈도(context window)라고 하며,
프롬프트와 생성 결과를 합친 길이가 이 한계를 넘을 수 없다.

## 환각 문제
LLM 은 학습 데이터에 없는 사실을 그럴듯한 문장으로 지어내는 환각(hallucination) 현상을 보인다.
모델이 확률적으로 가장 자연스러운 다음 토큰을 고를 뿐 사실 여부를 검증하지 않기 때문이다.
검색 증강 생성(RAG)은 답변에 필요한 근거 문서를 먼저 검색해 프롬프트에 넣어줌으로써
모델이 자기 기억 대신 주어진 자료에 근거하도록 유도해 이 문제를 완화한다.

## 추론 매개변수
생성 시점의 무작위성은 온도(temperature)로 조절한다. 0 에 가까우면 거의 결정적으로 가장 확률 높은 토큰을
고르고, 값이 커질수록 다양한 표현이 나온다. 자료를 그대로 옮겨야 하는 RAG 응답에는 낮은 온도가 적합하다.
상위 확률 누적합이 p 를 넘지 않는 후보만 남기는 top-p(뉴클리어스 샘플링)도 함께 쓰인다.`,
  },
  {
    title: "트랜스포머 (기계 학습)",
    url: "https://ko.wikipedia.org/wiki/트랜스포머_(기계_학습)",
    markdown: `## 개요
트랜스포머(Transformer)는 2017년 논문 "Attention Is All You Need" 에서 제안된 신경망 구조다.
순환 신경망(RNN)의 순차 처리 구조를 없애고 어텐션 연산만으로 문맥을 처리해,
시퀀스 전체를 병렬로 계산할 수 있게 만든 것이 핵심 기여다.

## 셀프 어텐션
셀프 어텐션(self-attention)은 각 토큰을 질의(Query), 키(Key), 값(Value) 세 벡터로 사영한 뒤,
질의와 키의 내적으로 토큰 간 관련도를 구하고 그 가중치로 값 벡터를 합산한다.
이 덕분에 문장 맨 앞의 단어와 맨 뒤의 단어가 거리에 상관없이 한 번의 연산으로 연결된다.
다만 연산량과 메모리가 시퀀스 길이의 제곱에 비례해 늘어나며, 이것이 긴 컨텍스트 처리의 병목이 된다.

## 위치 인코딩
어텐션 연산 자체는 토큰의 순서를 구분하지 못한다. 그래서 입력 임베딩에 위치 정보를 더해준다.
원 논문은 사인/코사인 함수를 쓰는 고정 위치 인코딩을 사용했고,
최근 모델은 질의와 키를 회전시켜 상대 위치를 표현하는 RoPE(Rotary Position Embedding)를 널리 쓴다.

## 인코더와 디코더
원 논문의 트랜스포머는 인코더와 디코더를 모두 가진 번역 모델이었다.
이후 계열이 갈라져 BERT 같은 인코더 전용 모델은 문장 이해와 임베딩 생성에,
GPT 같은 디코더 전용 모델은 다음 토큰 예측 기반의 문장 생성에 주로 쓰인다.
검색용 임베딩 모델은 대체로 인코더 전용 구조를 따른다.`,
  },
  {
    title: "단어 임베딩",
    url: "https://ko.wikipedia.org/wiki/단어_임베딩",
    markdown: `## 개요
임베딩(embedding)은 단어, 문장, 이미지 같은 대상을 고정 길이의 실수 벡터로 바꾼 표현이다.
의미가 비슷한 대상이 벡터 공간에서 가까이 놓이도록 학습되기 때문에,
벡터 사이의 거리를 재는 것만으로 의미적 유사도를 근사할 수 있다.

## 유사도 측정
가장 널리 쓰이는 척도는 코사인 유사도로, 두 벡터가 이루는 각도의 코사인 값을 쓴다.
벡터의 길이(크기)를 무시하고 방향만 비교하므로 문서 길이 차이에 덜 민감하다.
벡터 데이터베이스는 보통 1에서 코사인 유사도를 뺀 코사인 거리를 저장하며,
이 값이 0에 가까울수록 더 비슷하다는 뜻이다.

## 밀집 표현과 희소 표현
임베딩은 값 대부분이 0이 아닌 밀집(dense) 벡터다. 반대로 TF-IDF 나 BM25 는 어휘 사전 크기만큼의
차원 중 등장한 단어 자리만 값을 갖는 희소(sparse) 표현이다.
밀집 표현은 표현이 달라도 의미가 같으면 잡아내고, 희소 표현은 고유명사나 코드처럼
정확히 일치해야 하는 토큰에 강하다. 두 결과를 합치는 방식을 하이브리드 검색이라 한다.

## 다국어 임베딩
bge-m3 처럼 여러 언어를 하나의 벡터 공간에 함께 학습한 모델은
한국어 질문으로 영어 문서를 찾는 교차 언어 검색이 가능하다. bge-m3 의 출력 차원은 1024 이다.
임베딩 모델을 바꾸면 벡터 차원과 공간 자체가 달라지므로, 기존에 적재한 벡터는 재사용할 수 없고
전체 문서를 다시 임베딩해 적재해야 한다.`,
  },
]
