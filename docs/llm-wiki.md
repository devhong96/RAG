# LLM 위키 — 개념 사전 & 위키 코퍼스 실습

이 문서는 두 부분으로 되어 있습니다.

1. **[개념 위키](#1-개념-위키)** — RAG 를 다루면서 계속 마주치는 LLM 용어를 이 저장소의 코드와 연결해 정리했습니다.
2. **[위키 코퍼스 파이프라인](#2-위키-코퍼스-파이프라인-srcwiki)** — 실제 위키백과 문서를 받아 청킹·적재·질의하는 실습(`npm run wiki:*`)입니다.

---

## 1. 개념 위키

### 토큰 (Token)
LLM 은 글자가 아니라 **토큰** 단위로 텍스트를 읽고 씁니다. 영어는 단어 하나가 대략 1~2 토큰이지만,
한국어는 조사·어미 때문에 같은 의미라도 토큰이 2~3배 더 나옵니다.
"청크를 500자로 자른다"가 곧 "토큰 500개"가 아니라는 점을 기억하세요.

### 컨텍스트 윈도 (Context Window)
한 번의 요청에서 모델이 볼 수 있는 최대 토큰 수입니다. **프롬프트 + 생성 결과**를 합친 값이 여기에 들어가야 합니다.
RAG 에서 검색 결과를 너무 많이 넣으면 이 한계에 부딪히고, 한계에 닿지 않더라도 근거가 묻혀 답변 품질이 떨어집니다
(가운데 내용을 놓치는 *lost in the middle* 현상). 그래서 `nResults` 를 3~5 정도로 두고 재랭킹으로 추리는 것입니다.

### 임베딩 모델 vs 생성 모델
같은 Ollama 서버를 쓰지만 역할이 완전히 다릅니다. 이 저장소의 설정은 [`src/config.ts`](../src/config.ts) 한곳에 있습니다.

| 구분 | 이 저장소의 기본값 | 하는 일 | 코드 |
|:---|:---|:---|:---|
| 임베딩 모델 | `bge-m3` (1024차원) | 텍스트 → 벡터. 검색용 | [`ollama-embedding.ts`](../src/lib/ollama-embedding.ts) |
| 생성 모델 | `OLLAMA_CHAT_MODEL` (기본 `gemma4:26b`) | 근거를 읽고 답변 문장 생성 | [`llm.ts`](../src/lib/llm.ts) |

> ⚠️ **임베딩 모델을 바꾸면 벡터 공간 자체가 달라집니다.** 기존에 적재한 벡터는 재사용할 수 없으니
> `npm run reset` 후 전부 다시 적재해야 합니다. 반면 생성 모델은 언제든 바꿔도 됩니다.

### 온도 (Temperature)와 top-p
생성 시점의 무작위성을 조절합니다. `0` 에 가까우면 거의 결정적으로 가장 확률 높은 토큰을 고릅니다.
RAG 는 "자료에 있는 내용을 그대로 옮기는" 작업이므로 낮은 온도가 맞습니다.
그래서 [`chatComplete`](../src/lib/llm.ts) 의 기본값이 `temperature: 0` 입니다.

### 환각 (Hallucination)
모델이 사실이 아닌 내용을 그럴듯한 문장으로 지어내는 현상입니다.
확률적으로 자연스러운 다음 토큰을 고를 뿐 사실 여부를 검증하지 않기 때문에 생깁니다.
**RAG 는 환각을 없애는 기술이 아니라 줄이는 기술입니다.** 줄이는 수단은 세 가지입니다.

1. 좋은 근거를 검색해 넣는다 (청킹·하이브리드 검색·재랭킹)
2. "자료 밖의 내용을 쓰지 말라"는 규칙을 프롬프트에 넣는다
3. 답변에 출처를 밝히게 해서 사람이 검증할 수 있게 한다

### RAG 프롬프트 설계 세 가지 축
[`src/lib/rag.ts`](../src/lib/rag.ts) 의 시스템 프롬프트가 이 구조를 그대로 따릅니다.

| 축 | 내용 | 예시 문구 |
|:---|:---|:---|
| **역할** | 무엇을 하는 존재인지 | "주어진 자료를 바탕으로 답하는 한국어 도우미" |
| **근거 규칙** | 지어내지 못하게 | "자료에 근거가 없으면 '답할 수 없습니다'라고 답하세요" |
| **형식 규칙** | 길이·언어·출처 표기 | "세 문장 이내, 한국어, 마지막 줄에 자료 번호" |

### 문맥 보강 청킹 (Contextual Chunking)
문단 본문만 잘라서 임베딩하면 "이 값이 0에 가까울수록 비슷하다는 뜻이다" 같은 문장은
**무엇에 대한 이야기인지 벡터에 전혀 남지 않습니다.** 사람은 위쪽 제목을 보고 알지만 임베딩 모델은 넘겨준 글자만 봅니다.
그래서 청크 앞에 `문서 제목 > 절 제목` 한 줄을 붙입니다. 비용은 거의 없고 검색 품질은 눈에 띄게 올라갑니다.
아래 위키 파이프라인([`src/wiki/pipeline.ts`](../src/wiki/pipeline.ts))이 이 방식을 씁니다.

---

## 2. 위키 코퍼스 파이프라인 (`src/wiki/`)

한국어 위키백과 API 에서 문서를 받아 → 제목 계층을 살려 청킹하고 → Chroma 에 적재한 뒤 → 질문에 답합니다.
**직접 쓴 예제 문서가 아니라 진짜 문서**를 다루기 때문에, 부속 절 제거처럼 실무에서 실제로 필요한 처리를 함께 다룹니다.

### 실행 스크립트
| 스크립트 | 소스 파일 | 설명 |
|:---|:---|:---|
| `npm run wiki:ingest` | [`ingest.ts`](../src/wiki/ingest.ts) | 위키백과 문서 내려받기 → 구조 청킹 → `llm-wiki` 컬렉션 적재 |
| `npm run wiki:search` | [`search.ts`](../src/wiki/search.ts) | 유사도 검색 → 근거 조립 → LLM 답변 생성 |

```bash
npm run wiki:ingest                             # 기본 3개 문서 (LLM / 트랜스포머 / 워드 임베딩)
npm run wiki:ingest -- 파인_튜닝 어텐션_메커니즘   # 원하는 문서 제목 지정
npm run wiki:ingest -- --offline                # 네트워크 없이 로컬 사본으로 실습
npm run wiki:ingest -- --reset                  # 컬렉션을 비우고 처음부터 적재

npm run wiki:search                             # 기본 예시 질문
npm run wiki:search -- "워드 임베딩은 무엇인가요?"
```

### 내부 동작
| 단계 | 파일 | 하는 일 |
|:---|:---|:---|
| 1. 수집 | [`fetch.ts`](../src/wiki/fetch.ts) | 위키백과 `action=query&prop=extracts` 로 순수 텍스트 수집. `== 제목 ==` 을 마크다운 `##` 으로 변환 |
| 2. 폴백 | [`corpus.ts`](../src/wiki/corpus.ts) | 네트워크가 막히면 같은 주제의 로컬 사본으로 자동 대체 |
| 3. 청킹 | [`pipeline.ts`](../src/wiki/pipeline.ts) | 리드 문단을 `개요` 절로 분리 → 헤딩 단위 분할 → 500자 단위 재분할 → `제목 > 절` 접두어 부착 |
| 4. 적재 | [`pipeline.ts`](../src/wiki/pipeline.ts) | `출처 주소#순번` 을 id 로 upsert (재실행해도 중복 없음) |

### 이 실습에서 챙겨갈 점

* **부속 절 제거**: "각주", "외부 링크", "같이 보기" 같은 절은 링크와 서지 정보뿐이라 근거가 되지 못하면서
  검색 상위 자리는 차지합니다. `pipeline.ts` 의 `NOISE_HEADINGS` 에서 걸러냅니다.
  **인제스트 단계의 노이즈 제거는 청킹 전략만큼이나 검색 품질에 영향을 줍니다.**
* **넘겨주기(redirect) 처리**: `대규모 언어 모델` 로 요청하면 실제로는 `대형 언어 모델` 문서가 옵니다.
  검색 결과에 보이는 제목이 요청한 제목과 다를 수 있습니다.
* **동음이의 문서 주의**: `임베딩` 처럼 넘겨주기 목록만 있는 문서는 본문이 사실상 없습니다.
  그래서 기본 주제를 `단어 임베딩`(실제 문서명 `워드 임베딩`)으로 잡았습니다.
* **리드 문단을 놓치지 말 것**: 헤딩 기준으로만 자르면 첫 헤딩 앞에 있는 글이 어디에도 속하지 못해 버려집니다.
  위키백과 문서는 늘 정의 문단으로 시작하므로, 하필 **가장 답이 될 만한 문단**이 사라집니다.
  `pipeline.ts` 의 `splitWithLead` 가 이 부분을 `개요` 절로 떼어내 살립니다.
* **타임아웃은 필수**: `AbortSignal.timeout()` 없이 외부 API 를 부르면 응답 없는 네트워크에서 스크립트가 무한정 멈춥니다.

### 이어서 볼 것
* 청킹 전략 비교 → [`docs/lectures.md`](lectures.md) 의 15강(청킹 전략 6종), 27강(Parent-Child)
* 검색 품질 개선(하이브리드 19강 · 재랭킹 29~30강) → [`docs/rag-architecture.md`](rag-architecture.md)
